<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd http://www.springframework.org/schema/tx 
		http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.glodon.paas.account.dao.mapper" />
		<property name="sqlSessionFactoryBeanName" value="sqlSessionFactory" />
	</bean>

	<bean id="mysqlDataSource" class="com.alibaba.druid.pool.DruidDataSource"
		destroy-method="close">
		<property name="url" value="${account.mysql.url}" />
		<property name="username" value="${account.mysql.username}" />
		<property name="password" value="${account.mysql.password}" />
		<property name="driverClassName" value="${account.mysql.driverClassName}" />
		<property name="maxActive" value="${account.mysql.pool.maxActive}" />
		<property name="minIdle" value="${account.mysql.pool.minIdle}" />
		<property name="testWhileIdle" value="${account.mysql.pool.testWhileIdle}" />
		<property name="testOnReturn" value="${account.mysql.pool.testOnReturn}" />
		<property name="testOnBorrow" value="${account.mysql.pool.testOnBorrow}" />
		<property name="timeBetweenEvictionRunsMillis"
			value="${account.mysql.pool.timeBetweenEvictionRunsMillis}" />
		<property name="minEvictableIdleTimeMillis"
			value="${account.mysql.pool.minEvictableIdleTimeMillis}" />
		<property name="validationQuery" value="select 1" />
	</bean>
	<bean id="sqlSessionFactory" name="sqlSessionFactory"
		class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="mysqlDataSource" />
	</bean>

	<!-- 事务管理器配置 -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="mysqlDataSource" />
	</bean>

	<!-- 使用annotation定义事务 -->
	<tx:annotation-driven transaction-manager="transactionManager" />

</beans>
